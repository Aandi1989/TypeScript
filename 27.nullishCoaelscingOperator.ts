let speed: number | null = null;
let ride = {
    // Nullish coalescing operator 
    // if speed is not nol or undefined, use that value. Otherwise, use 30 as a default value.
    speed: speed ?? 30
}